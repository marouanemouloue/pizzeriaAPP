import { PizzaIngredientColorPipe } from './pizza-ingredient-color.pipe';

describe('PizzaIngredientColorPipe', () => {
  it('create an instance', () => {
    const pipe = new PizzaIngredientColorPipe();
    expect(pipe).toBeTruthy();
  });
});
